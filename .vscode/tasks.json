{
	"version": "2.0.0",
	"tasks": [
        {
            "type": "shell",
            "label": "build netconfd",
            "command": "go build",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/"
            },
            "problemMatcher": [
                "$go"
            ],
            "group": "build"
        }
    ]
}